name: simple_markdown
description: "A Flutter package for parsing and rendering markdown using low-level rendering APIs."
version: 0.1.0
homepage: https://github.com/tksuns12/simple_markdown

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
# This package does not include any Flutter-specific assets or fonts.
flutter: null
